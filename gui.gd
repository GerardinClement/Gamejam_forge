extends Control

func _ready():
	pass
	#var textureRect = self.get_child(1)
	#for heart in player.pv:
		#textureRect.duplicate()


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
